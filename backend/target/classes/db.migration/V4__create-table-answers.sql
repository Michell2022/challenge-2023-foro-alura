CREATE TABLE answers (
    ID INT PRIMARY KEY AUTO_INCREMENT,
    ANSWER TEXT NOT NULL,
    CREATED_AT DATETIME NOT NULL,
    USER_ID INT,
    TOPIC_ID INT,
    FOREIGN KEY (TOPIC_ID) REFERENCES users(ID),
    FOREIGN KEY (TOPIC_ID) REFERENCES topics(ID)
);